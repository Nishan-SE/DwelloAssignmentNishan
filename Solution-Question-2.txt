
SELECT DEPARTMENT.NAME , count(EMPLOYEE.ID) AS 'NO OF EMPLOYEE'
FROM DEPARTMENT 
LEFT JOIN EMPLOYEE
ON  EMPLOYEE.DEPT_ID= DEPARTMENT.ID 
GROUP BY DEPARTMENT.NAME
ORDER BY COUNT(Employee.id) DESC, DEPARTMENT.NAME